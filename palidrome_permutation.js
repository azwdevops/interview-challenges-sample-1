function palidrome_permutation(str) {
  const arr = str.replace(/ /g, "").split("");
  const new_arr = [];
  for (let i = 0; i < arr.length; i++) {
    new_arr.push();
  }
}

palidrome_permutation("");
